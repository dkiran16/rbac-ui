import UserLogin from "./UserLogin";

const HomePage = () =>{
    return (
        <div>
            <h2>This RBAC -- Role Based Access Controle System</h2>
            <p>Admin can manage Roles and Users like Adding new Roles, edit existing roles , also can Register the User and assign a role to a User</p>
        </div>
    );
}

export default HomePage;